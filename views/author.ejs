<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>
			<% if (author) {%> <%= author[0].name %> <% } else {%> Author not found <%
			}%>
		</title>
	</head>
	<body>
		<% if (author) {%>
		<h1>List of books written by <%= author[0].name %></h1>
		<ul>
			<% author.forEach((book) => {%>
			<li>
				<a href="/books/<%= book.id%>"><%= book.title %></a> | <%=
				book.publication_year %> | <%= book.genre_name %>
			</li>
			<% }) %>
		</ul>
		<% } else {%>
		<h1>Author not found.</h1>
		<% } %>
		<a href="/authors">Return to authors</a>
	</body>
</html>
